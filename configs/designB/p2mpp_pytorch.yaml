# PyTorch Pixel2Mesh++ Configuration
# Optimized for RTX 4070 GPU

# Model architecture
hidden_dim: 192
coord_dim: 3
feat_dim: 2883
stage2_feat_dim: 339

# Training parameters (for future use)
batch_size: 1
epochs: 10
lr: 0.00001
seed: 123

# GPU settings
gpu_id: 0

# Paths
name: "refine_p2mpp_pytorch"
save_path: "artifacts/checkpoints/torch"

# Testing parameters
test_epoch: 10
restore: True

# Input settings
is_voxel_input: False
is_debug: False
load_cnn: True

# CNN parameters
cnn_step: 50
init_epoch: 50
pre_trained_cnn_path: "artifacts/checkpoints/torch/coarse_mvp2m/models"

# Test paths (will be overridden by command line args)
test_file_path: "data/test_list.txt"
test_data_path: "data/demo/p2mppdata/test"
test_image_path: "data/demo/rendering_only"
test_mesh_root: "artifacts/outputs/pytorch_gpu/meshes"

# Training paths (not used for inference)
train_file_path: "data/train_list.txt"
train_data_path: "/home/your_user_name/data/ShapeNetModels/train"
train_image_path: "/home/your_user_name/data/ShapeNetImages/ShapeNetRendering"
train_mesh_root: "results/coarse_mvp2m/coarse_intermediate/50"
