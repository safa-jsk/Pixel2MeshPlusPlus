# GPU Environment for Design B - RTX4070
# TensorFlow 2.10.1 with GPU support (CUDA 11.8, cuDNN 8.1)

# Note: TensorFlow is already in base image

# Core dependencies (compatible versions with TF 2.10.1 and Python 3.8)
numpy>=1.19,<1.24
scipy>=1.7,<1.10
matplotlib>=3.5,<3.7
scikit-image>=0.19,<0.20
Pillow>=9.0,<10.0

# Utilities
tqdm>=4.60
pyyaml>=5.4

# Deep learning utilities
tflearn>=0.5.0
